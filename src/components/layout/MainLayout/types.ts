// Currently MainLayout has no props, but we create this file for consistency and future extensibility.
// Currently MainLayout has no props, but we create this file for consistency and future extensibility.
// eslint-disable-next-line @typescript-eslint/no-empty-object-type
export interface MainLayoutProps {}
